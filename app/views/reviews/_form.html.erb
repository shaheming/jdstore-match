<%= simple_form_for([product, product.reviews.build]) do |f|%>
  <div class="review-field">
      <%= f.text_area :body, class: "form-control" %>
  </div>
 	 <%= f.submit "Add a Review", class: "btn btn-primary pull-right",id:"cart-botton" %>
<% end %>
